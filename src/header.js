import React from "react";

class Header extends React.Component {
  render() {
    return (
      <header id="header"> Watch the counter REACT to the buttons! </header>
    );
  }
}

export default Header;
